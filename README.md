# please-line-up-the-slippers-dad

## How to run

1. Clone the repository
2. Connect to SORACOM and configure SIM groups
3. Install the dependencies:

   ```bash
   sudo apt install libcap-dev
   pip install -r requirements.txt

4. Run the script:

   ```bash
    python main.py
    ```
